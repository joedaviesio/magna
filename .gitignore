# Environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Node
node_modules/
.next/
out/

# Data (large files)
data/raw/*.xml
data/embeddings/*.pkl
# embeddings tracked via Git LFS (see .gitattributes)
# data/embeddings/embeddings.npy
# data/embeddings/metadata.json
data/processed/chunks/all_chunks.json
# Release-delivery embeddings build artifacts — uploaded to a GitHub Release,
# never committed (keeps them out of git/LFS). See Dockerfile + shard_embeddings.py
data/embeddings/embeddings_f16.npy
data/embeddings/embeddings_f16.npy.part*

# IDE
.vscode/
.idea/

# Images (logos, portraits)
*.png
*.jpg
*.jpeg

# OS
.DS_Store
Thumbs.db

# Style guide (portable, not project code)
STYLE_GUIDE.md

# Local logs
logsweek1
logs/
backend/logs/
.gstack/
