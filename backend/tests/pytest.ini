[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = .
addopts = -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
markers =
    golden: golden retrieval set checks (R2)
    slow: takes more than a couple of seconds
